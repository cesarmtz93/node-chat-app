{{> header}}
<div class="container">
    <header>
        <div class="page-header">
            <h1>{{pageTitle}}</h1>
            <p>{{upperCase pageParagraph}}</p>
        </div>
    </header>
</div>

{{> footer}}
